---
interface Props {
  id?: string;
}

const { id } = Astro.props;
---

<div
  id={id}
  class="w-full h-[calc(100vh-3.5rem)] bg-darkp p-8"
>
  <section>
    <h1 class="text-center text-3xl sm:text-4xl font-medium tracking-wide">Projects</h1>
  </section>
</div>
